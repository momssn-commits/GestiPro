# =============================================================================
# GestiPro — Variables d'environnement frontend PRODUCTION
# Copier vers frontend/.env.local sur le serveur
# =============================================================================

# URL de l'API backend (accessible publiquement via Nginx)
NEXT_PUBLIC_API_URL=https://VOTRE_DOMAINE_OU_IP/api
